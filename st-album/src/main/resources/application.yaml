spring:
  application.name: st-album
  #profiles.active: dev
  datasource:
      url: jdbc:postgresql://localhost:5432/st_album
      username: stalbum
      password: 123456
  jpa:
      hibernate.ddl-auto: update
      properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

ribbon:
  enabled: true

server.port: 0